[tool.ruff]
target-version = "py38"

[tool.ruff.lint]
select = [
    "E", "W",       # pycodestyle
    "F",            # pyflakes
    "I",            # isort
    "N",            # pep8-naming
    "PLC", "PLE",   # pylint
    "UP",           # pyupgrade
]
